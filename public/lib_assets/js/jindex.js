window.app = window.app || {};
app.renderData = function(){
    for (let key in app.fragments) {
        const fragment = app.fragments[key];
        
    }
    
};
/*----------------------------------------------------------------------------*/
